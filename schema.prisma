datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model ProxyToken {
  id      Int    @id @default(autoincrement())
  content String
}
