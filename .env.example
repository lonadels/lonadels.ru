# Copy this file to .env and fill in secure values. Do NOT commit your real .env
# PostgreSQL connection settings
# If you set POSTGRES_* only, docker-compose will construct DATABASE_URL for main-app automatically.
# Alternatively, you can set DATABASE_URL explicitly (it will override the constructed one).
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
DATABASE_URL=
